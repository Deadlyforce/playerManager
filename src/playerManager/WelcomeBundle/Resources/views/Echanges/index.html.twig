{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Echanges list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Prospects_id</th>
                <th>Date</th>
                <th>Contenu</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('echanges_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.prospectsid }}</td>
                <td>{% if entity.date %}{{ entity.date|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>{{ entity.contenu }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('echanges_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('echanges_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('echanges_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
