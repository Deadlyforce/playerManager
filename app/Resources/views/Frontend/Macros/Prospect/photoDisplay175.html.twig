{% macro primaryPhoto(photos) %}
    
    {% set primaryPhoto = null %} 
    {% for photo in photos if photo.selected == true %}                               
        {% set primaryPhoto = photo %}                           
    {% endfor %}   
    
    {% if primaryPhoto != null %}
        <img src="{{ primaryPhoto.path | imagine_filter('thumb_prospect_175') }}" alt="Photo" />    
    {% else %}
        <img src="{{ asset('bundles/app/images/prospect_no_photo.jpg') }}" alt="No photo" />
    {% endif %}
{% endmacro %}



