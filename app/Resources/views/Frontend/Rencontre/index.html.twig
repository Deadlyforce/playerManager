{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Rencontre list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Date</th>
                <th>Lieu</th>
                <th>Duree</th>
                <th>Depensestotal</th>
                <th>Changementlieu</th>
                <th>Listelieux</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('rencontre_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{% if entity.date %}{{ entity.date|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>{{ entity.lieu }}</td>
                <td>{{ entity.duree }}</td>
                <td>{{ entity.depensesTotal }}</td>
                <td>{{ entity.changementLieu }}</td>
                <td>{{ entity.listeLieux }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('rencontre_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('rencontre_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('rencontre_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
