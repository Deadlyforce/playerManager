{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Encounters list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Date</th>
                <th>Place</th>
                <th>Duration</th>
                <th>Total expenses</th>
                <th>Venue Change</th>
                <th>Venues list</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('encounter_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{% if entity.date %}{{ entity.date|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>{{ entity.place }}</td>
                <td>{{ entity.duration }}</td>
                <td>{{ entity.expenses }}</td>
                <td>{{ entity.venueChange }}</td>
                <td>{{ entity.venuesList }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('encounter_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('encounter_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('encounter_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
