{% extends "::base.html.twig" %}
{% block title %}{{ 'app.register.confirmed.title'|trans }}{% endblock %}


{% trans_default_domain 'FOSUserBundle' %}

{% block content %}
    <div class="container">
        <section class="page-title">
            <h1>{{ 'app.register.confirmed.title'|trans }}</h1> 
{#            <h2></h2>#}
        </section>
        <div class="page-title-underline"></div>       
        
        <p class="confirmed">{{ 'registration.confirmed'|trans({'%username%': user.username}) }}</p>
        <a class="btn-start"  href="{{ path('home_index') }}" title="Home"><i class="icon ion-ios-home-outline"></i>{{ 'app.register.confirmed.start'|trans }}</a>
        
        
        {% if targetUrl %}
{#            <p><a href="{{ targetUrl }}">{{ 'registration.back'|trans }}</a></p>#}
        {% endif %}
    </div>
{% endblock %}
